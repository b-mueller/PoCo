property statusCompleted {
	always(
		(
			Hub.m_tasks[1].status == COMPLETED
			&&
			prev(Hub.m_tasks[1].status) != COMPLETED
		)
		=>
		(
			FUNCTION == Hub.finalize(bytes32,bytes)
			&&
			1 == Hub.finalize(bytes32,bytes)[0]
			&&
			prev(Hub.m_tasks[1].status) == REVEALING
		)
	);
}
